# Terraform

This folder includes all the files required to setup the cloud providers.

To setup the cloud services required for the project, run:
`terraform apply`

To destroy the cloud services created in this project, run:
`terraform destroy`